// Runtime stub for the claude-code host API (injected at runtime by Claude Code).
// Actual behaviour is injected by the host; this file resolves the module for offline bun test.
export const stub = true;
