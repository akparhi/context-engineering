name = "explorer"
description = "Fast, read-only code mapping, symbol tracing and locating relevant tests."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
web_search = "cached"
sandbox_mode = "read-only"
developer_instructions = "Map the smallest relevant code path. Read targeted files and use bounded searches. Return file:line evidence, behavior, relevant tests and unknowns. Do not edit files or spawn agents."

[agents]
enabled = false
